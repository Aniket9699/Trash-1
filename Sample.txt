@echo off

set timestamp=${p:Date}

REM Extract current date components
for /f "tokens=2 delims==" %%A in ('wmic os get LocalDateTime /value ^| findstr "="') do set "datetime=%%A"
set "year=%datetime:~0,4%"
set "month=%datetime:~4,2%"
set "day=%datetime:~6,2%"

REM Format date components into a date string
set "date_str=%year%-%month%-%day%"

echo %date_str%

if exist "%filename%" (
    echo File exists
) else (
    echo File does not exist
)


----------------------------------------------------------------
def convertTimestampToDate(timestamp) {
    def date = new Date(timestamp)
    def formattedDate = date.format("d MMM yyyy HH:mm:ss")
    return formattedDate
}


def convertToDate(timestamp) {
    def date = new Date(timestamp)
    def formattedDate = date.format("d MMM yyyy")
    return formattedDate
}

def timestamp = ${p:Date}

def formattedDate = convertTimestampToDate(timestamp)
println("Date: $formattedDate")
def Date = convertToDate(timestamp)
println("Date: $Date")

===================================================================================

Application_Name
Release_ID
Deployed_Artifacts
Method_of_Deployment
Environment
Live_Or_nonLive
Deployment_Or_Activity
Ticket_Type
Ticket_Number
StartTime
EndTime

====================================================================================

def convertTimestampToDate(timestamp) {
    def date = new Date(timestamp)
    def formattedDate = date.format("d MMM yyyy hh:mm:ss a")
    return formattedDate
}

def convertToDate(timestamp) {
    def date = new Date(timestamp)
    def formattedDate = date.format("d MMM yyyy")
    return formattedDate
}

def convertToTime(timestamp) {
    def date = new Date(timestamp)
    def formattedDate = date.format("hh:mm:ss a")
    return formattedDate
}

def timestamp = ${p:StartTime}
def formattedDate = convertToDate(timestamp)
println("StartDate: $formattedDate")

def timestamp3 = ${p:StartTime}
def formattedTime = convertToTime(timestamp3)
println("StartTime: $formattedTime")

def timestamp2 = ${p:EndTime}
def formattedDate2 = convertToDate(timestamp2)
println("EndDate: $formattedDate2")

def timestamp1 = ${p:EndTime}
def formattedTime1 = convertToTime(timestamp1)
println("EndTime: $formattedTime1")

def inputString = "${p:Release_ID}"
def outputString = inputString.replace(",","^|")
println("ReleaseID: $outputString")

def inputString1 = "${p:Ticket_Number}"
def outputString1 = inputString1.replace(",","^|")
println("Ticket Number: $outputString1")


